apiVersion: v1
kind: Pod
metadata:
  name: custom-scheduler-pod
spec:
  schedulerName: custom-scheduler # 指定调度器的名字
  containers:
    - name: my-container
      image: nginx:latest
      ports:
        - containerPort: 80